use font_parser::BezierCurve;
use image::ImageBuffer;


pub fn entry(shape: &Vec<Vec<BezierCurve>>, atlas_entry:) {
    let height = 32;
    let width = 32;
    let mut img = ImageBuffer::new(32, 32);
}
pub fn preprocess(shape: &Vec<Vec<BezierCurve>>) { 
    
}
